# Things that still must be arranged or decided

- When `include-pick-id` and/or `include-pick-classes` are specified more than once for
  the same external document, the resulting `included-id` is the same, while it should
  be different for every inclusion

- support for the [include-files](https://github.com/pandoc-ext/include-files)
  filter conventions